#include "dominion.h"
#include <stdio.h>
#include <math.h>
#include <stdlib.h>

void myassert(){
	
}

//compare(const void* a, const void* b)

int main(){
	compare(0, 1);
	assert();
	compare(1, 0);
	assert();
	compare(0, 0);
	assert();
}